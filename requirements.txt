# DetControl / Full Scale Facility — install in the same Python env the IDE uses:
#   py -m pip install -r requirements.txt
alicat>=0.9.0
nidaqmx>=1.5.0
numpy>=2.1.3
PySide6>=6.11.0
pyserial>=3.5
pyttsx3>=2.99
